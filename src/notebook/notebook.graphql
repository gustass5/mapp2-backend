type Query {
	notes(id: String): [Note]
}

type Mutation {
	createNote(input: CreateNoteInput!): Note!
}

type Note {
	id: Int!
	headline: String
	content: String
	creationDate: String
}

input CreateNoteInput {
	headline: String!
	content: String!
	creationDate: String!
}
